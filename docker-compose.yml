services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: scale
      POSTGRES_PASSWORD: scale
      POSTGRES_DB: scale
    ports:
      - '5432:5432'
    volumes:
      - scale_pg:/var/lib/postgresql/data

volumes:
  scale_pg:
